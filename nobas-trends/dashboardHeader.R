header <- dashboardHeader(title = 'CUNY BPL Trend Chart App',
                          titleWidth = 450,
                          tags$li(
                            class = "dropdown",
                            actionButton('HomeTab',"Home")
                          ))
