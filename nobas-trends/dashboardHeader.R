header <- dashboardHeader(title = 'CUNY BPL NoBAS Trending Tool',
                          titleWidth = 450,
                          tags$li(
                            class = "dropdown",
                            actionButton('HomeTab',"Home")
                          ))
